To build documentation, first install sphinx:

```
pip install sphinx
```

Then, run:

```
make html
```

In this folder. This will produce a new folder called "build" containing documentation in HTML.
